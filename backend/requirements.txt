# ─── Core Framework ──────────────────────────────────
crewai[google-genai]
fastapi
uvicorn[standard]
python-multipart
python-dotenv
google-generativeai        # Gemini (free tier) - replaces Anthropic

# ─── Animation & Video ───────────────────────────────
manim
moviepy>=2.0                # v2 API: import from moviepy directly
ffmpeg-python

# ─── Content Extraction ──────────────────────────────
pymupdf
PyPDF2
pytesseract
Pillow

# ─── Text-to-Speech ──────────────────────────────────
lmnt                       # LMNT (primary — high quality)
edge-tts                   # Microsoft Edge neural voices (free fallback)
gTTS                       # Google TTS (free fallback)

# ─── Web Interface ───────────────────────────────────
gradio

# ─── Optional Enhancements ──────────────────────────
aiohttp                    # Fetch.ai integration (optional)
opencv-python              # Enhanced image processing (optional)
pdf2image                  # PDF-to-image for Gemini Vision OCR (optional)
pydantic                   # Data models
